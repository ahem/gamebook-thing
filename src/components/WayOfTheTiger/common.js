// @flow
import * as React from 'react';
import styled from 'styled-components';

export const font = `small-caps normal 16px serif`;
export const phoneMedia = `only screen and (min-device-width: 375px) and (max-device-width: 667px)`;

export const Text = styled.span`
    font: ${font};
`;
